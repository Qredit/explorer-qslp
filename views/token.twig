{% extends 'layout_sub.twig' %}

{% block body %}
	{% include 'layout_header_sub.twig' %}
	<div class="container" style="margin-top:50px;margin-bottom:50px;">
		<div class="container">
			<div class="container search-title" id="aftersearch"></div>
			<div class="banner-servers-box">
				<div class="counter-placeholder"></div>
				<div class="banner-text-left">
					<h4 style="color:#fff;">
						<strong>QAE Tokens</strong>
					</h4>
					<p>QAE tokens on the Qredit Blockchain</p>
				</div>
			</div>

			<div class="table-responsive">

				<table class="table">


					<thead>
						<tr>
							<th class="server-tabls-head" scope="col">Data</th>
							<th class="server-tabls-head" scope="col">Output</th>
						</tr>
					</thead>
					<tbody id='tokeninfo' class="server-tabls-row"></tbody>


				</table>
			</div>
		</div>
	</div>
</div></div></div></div{{tokenid}}<script>{% include 'layout_footer_sub.twig' %}{% endblock %}{% block javascripts %}$(document).ready(function() {
		
			var socket = io();
			
			socket.emit('gettokeninfo', {tokenid: '{{ tokenid }}'} );
			
		  	socket.on('showtokeninfo', function (data) { 
		  	
		  		var mainTable = document.getElementById("tokeninfo");
		  		var tr, td;
		
		  		data.forEach(function(row) {
		  		
		  			tr = document.createElement("tr");
		  			
		  			td = document.createElement("td");
		  			td.innerHTML = row.symbol;
					tr.appendChild(td);
					  
		  		  	td = document.createElement("td");
		  			td.innerHTML = row.paused;
		  			tr.appendChild(td);
		  			
		  			tokeninfo.appendChild(tr);
		  		
		  		});
		
		  	});
		
		});</script>{% endblock %}
